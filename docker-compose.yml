services:
  personal-mcp:
    build:
      dockerfile: Dockerfile
    env_file: .env
    restart: unless-stopped
    networks:
      - caddy
    labels:
      caddy: personal-mcp.nikitarusin.fun
      caddy.reverse_proxy: "{{upstreams 8080}}"

networks:
  caddy:
    external: true